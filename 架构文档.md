# 深网内容爬虫系统架构（基于 crawl4AI + 盘古/DeepSeek 多模型协同）
version: 0.4
status: "RC_READY" # Release Candidate Ready

ai_entry_point:
  read_order:
    - "1. 目标与范围"
    - "2. 整体架构概览"
    - "7. 系统创新点"
    - "5. 当前进度 & 下一步计划"
    - "6. 更新记录（最近一条）"
  next_action_procedure:
    - "步骤1：阅读 5.1 的任务列表，确认哪些已完成、哪些未完成。"
    - "步骤2：优先查看 5.2 中的 NEXT_ACTION，如果存在则尝试完成它。"
    - "步骤3：完成任务后，更新 5.1 勾选状态（[ ] -> [x]），并在 6. 更新记录 追加一条记录。"
    - "步骤4：如有新的后续任务，在 5.1 追加新的待办项，并在 5.2 更新新的 NEXT_ACTION。"
  write_back_rules:
    - "仅在完成实质性工作时更新文档，不要为纯思考过程写更新记录。"
    - "更新记录必须包含：时间、执行主体、做了什么、剩余问题/限制。"
    - "保持原有内容不被随意删除，如需废弃某设计，在更新记录中说明原因。"

---

## 1. 目标与范围

### 1.1 核心目标

- G1: 支持按主题进行深网内容爬取（Topic-based crawling）。
- G2: 支持按 URL 进行深网内容爬取（URL-based crawling）。
- G3: 爬取前可通过多智能体（Explorer/Critic）对话生成/优化爬取计划。
- G4: 爬取后对结果进行结构化提取（提取为 JSON 表格）及情报总结。
- G5: 支持多模态内容爬取（发现并记录图片/视频等媒体信息）。
- G6: **[NEW]** 支持基于统计学的网页去噪与 Schema 自动发现，实现“意图驱动”的精准提取。

### 1.2 非目标（当前阶段）

- N1: 暂不实现跨机器的分布式调度，但在单机层面实现了多 Key 高并发轮询。
- N2: 暂不固定具体数据库，目前使用 PostgreSQL (JSONB) 存储所有非结构化与半结构化数据。

---

## 2. 整体架构概览

### 2.1 模块划分（Tech Stack Update）

1. **API Gateway / Orchestrator**: Python (FastAPI)
   - 核心后端，负责请求路由、任务调度、状态管理及历史记录查询。
2. **LLM Planner (Multi-Agent Council)**: Python Client
   - **Explorer (Qwen-72B)**: 发散思维，负责发现所有潜在链接与路径。
   - **Critic (DeepSeek-V3)**: 审视思维，负责过滤广告与无关链接，确保爬虫不跑偏。
   - **Moderator (Pangu-Pro)**: 综合决策，下达最终爬取指令。
3. **Crawler Engine**: Python (crawl4AI / Playwright)
   - 纯异步爬虫核心，负责执行具体页面的渲染和抓取。
4. **Structure & Schema Engine**: Python
   - **Structure Scorer**: 对网页结构化程度打分，识别页面类型。
   - **Schema Discovery**: 基于用户意图（User Request）自动推导数据 Schema。
   - **Statistical Denoising**: 基于文档频率（Document Frequency）物理切除导航栏与页脚。
   - **Extractor**: 支持多 Key 轮询与多模型自动降级（Fallback）的高并发提取器。
5. **Storage Layer**: PostgreSQL
   - 使用 `JSONB` 字段存储 CrawlPlan、PageCrawlResult 和 ExtractedData。
6. **Result Analyzer**: Python (后台异步任务)
   - 负责对爬取回来的原始数据进行最终的情报简报生成。
7. **Frontend**: Vue.js 3 + Element Plus
   - **控制台**: 支持“指挥中心”直播模式（显示 AI 辩论日志）。
   - **数据透视**: 支持结构化数据表格展示、去噪前后网页对比、JSON 导出。

### 2.2 逻辑数据流

```text
[User / Vue Client]
        | (HTTP/REST)
        v
[FastAPI Gateway]
        |
        +--> [LLM Planner - Multi-Agent Debate] --> CrawlPlan (JSON + Debate Logs)
        |
        v
[Crawl Scheduler] --> URL 队列 --> [Crawler Engine]
        |
        v
[Raw Page Results] --> [https://stats.stackexchange.com/questions/110428/clustering-a-noisy-data-or-with-outliers](https://stats.stackexchange.com/questions/110428/clustering-a-noisy-data-or-with-outliers)
        |
        +--> [Schema Discovery] (Schema w/ Negative Constraints)
        |
        +--> [Structured Extractor] (Multi-Key & Model Fallback) --> Extracted Data (JSON)
        |
        v
[Result Analyzer] --> Final Summary
        |
        v
[User / Vue Client (Table View & Export)]
````

-----

## 3\. 数据与协议（JSON 结构）

### 3.1 CrawlPlan (扩充)

```jsonc
{
  "plan_id": "string",
  "topic": "string",
  "seed_urls": ["string"],
  "max_depth": 2,
  "max_pages": 100,
  "debate_logs": "string (Markdown log of AI council)",
  "schemas": [
    {
       "template_pattern": "/news/{id}",
       "entity_type": "news_article",
       "fields": [...]
    }
  ],
  "structured_extract": {
     "task_id": "...",
     "templates": [
        {
           "template_pattern": "...",
           "records": [{"field1": "val1"}, ...]
        }
     ]
  }
}
```

### 3.2 CrawlTask

```jsonc
{
  "task_id": "string",
  "plan_id": "string",
  "status": "PENDING",
  "mode": "topic",
  "request_type": "CREATE_PLAN_AND_RUN",
  "user_request": "string",
  "created_at": "ISO8601",
  "updated_at": "ISO8601"
}
```

### 3.3 PageCrawlResult

```jsonc
{
  "task_id": "string",
  "plan_id": "string",
  "url": "string",
  "status": "SUCCESS",
  "http_status": 200,
  "markdown": "string",
  "html": "string",
  "links": {
    "internal": ["string"],
    "external": ["string"]
  },
  "media": {
    "images": ["string"],
    "videos": ["string"],
    "audios": ["string"],
    "structure": {
       "local_structure_score": 0.8,
       "template_score": 0.9,
       "structure_score": 0.85
    },
    "cleaned_markdown": "string (去噪后的文本，供前端展示)"
  },
  "meta": {
    "title": "string",
    "language": "string",
    "detected_topic": "string"
  },
  "error_message": "string | null",
  "fetched_at": "ISO8601"
}
```

### 3.4 TaskAnalysisResult

```jsonc
{
  "task_id": "string",
  "plan_id": "string",
  "summary": "string",
  "key_points": ["string"],
  "source_types": ["string"],
  "quality_score": 0,
  "quality_comment": "string",
  "next_crawl_suggestions": ["string"],
  "created_at": "ISO8601"
}
```

-----

## 4\. 服务接口（逻辑）

### 4.1 创建并启动爬取任务

```http
POST /tasks
```

### 4.2 查询任务状态

```http
GET /tasks/{task_id}
```

*Response 包含 `debate_logs`, `results`, `summary`, `crawled_count`, `structured_data` 等字段。*

### 4.3 获取任务列表（历史记录）

```http
GET /tasks
```

-----

## 5\. 当前进度 & 下一步计划

### 5.1 功能模块状态

#### 后端核心 (Backend)

  - [x] A-01 架构设计 & A-02 技术选型 & A-03 数据库 Schema
  - [x] B-01 盘古 Client 封装
  - [x] **B-02 LLM Planner** (多智能体决策委员会: Explorer/Critic/Moderator)
  - [x] **B-03 Crawl Scheduler** (分层递归 + AI 辩论调度)
  - [x] **B-04 crawl4AI 接入与封装**
  - [x] **B-05 数据存储** (PostgreSQL + JSONB)
  - [x] **B-06 Result Analyzer** (基于结构化评分的加权总结)
  - [x] **D-02 结构化抽取器** (Schema 自动发现 + 统计学去噪 + 高并发提取)
  - [x] **STRUCTURE-01 结构化探测** (Local + Template 评分系统)

#### 服务与监控 (Service)

  - [x] C-01 API Gateway 基础工程
  - [x] C-02 基础监控与日志 (已实现 Key 健康检查脚本 `check_keys.py`)

#### 前端 (Frontend)

  - [x] D-01 前端项目初始化
  - [x] **D-03 深度数据展示** (已完成：终端直播风格、结构化表格、去噪对比、JSON导出、历史记录)

### 5.2 下一步推荐行动（NEXT\_ACTION）

后端与前端核心功能均已闭环，下一步主要是系统的长期维护性与部署优化。

NEXT\_ACTION:
id: "F-01"
title: "Docker 化部署与生产环境加固"
description: |

1.  **Dockerfile 优化**: 编写 `Dockerfile`，将 Python 后端与 Vue 前端打包（或分离部署）。
2.  **Playwright 环境**: 确保 Docker 容器内包含 Playwright 所需的浏览器依赖。
3.  **配置分离**: 确保 `.env` 文件在生产环境中能通过 Volume 或环境变量注入。
    depends\_on: ["E-01"]
    expected\_output: "一个 `docker-compose up` 命令即可拉起包含前端、后端、数据库的完整系统。"

### 5.3 现有项目架构

```text
### 5.3 现有项目架构

```text
📂 项目架构: PanguCrawl
├── app
│   ├── services
│   │   ├── analyzer.py          # [DONE] 结果分析与总结 (Result Analyzer)
│   │   ├── crawler.py           # [DONE] 网页抓取核心 (Crawler Engine)
│   │   ├── extractor.py         # [DONE] 结构化提取器 (Structured Extractor)
│   │   ├── llm.py               # [DONE] 大模型客户端封装
│   │   ├── planner.py           # [DONE] 决策委员会 (Planner)
│   │   ├── schema_discovery.py  # [DONE] Schema 自动发现
│   │   ├── structure_scorer.py  # [DONE] 结构化评分系统
│   │   └── template_scorer.py   # (保留/已废弃) 旧版评分逻辑
│   ├── __init__.py
│   ├── config.py                # [DONE] 配置加载 (含 Key Pool)
│   ├── database.py              # [DONE] 数据库连接
│   ├── main.py                  # [DONE] API 入口与路由
│   ├── models.py                # [DONE] 数据库模型定义
│   └── utils.py                 # [DONE] 通用工具函数
├── web-ui
│   ├── public
│   ├── src
│   │   ├── router
│   │   ├── views
│   │   │   └── HomeView.vue     # [DONE] 核心控制台页面
│   │   ├── App.vue
│   │   └── main.js
│   ├── package.json
│   ├── vite.config.js
│   └── ... (其他前端配置)
├── .env                         # 环境变量 (Key, DB URL)
├── check_keys.py                # [NEW] API Key 健康检查脚本
├── docker-compose.yml           # 容器编排配置
├── init_db.py                   # 数据库初始化脚本
├── readme.md                    # 项目说明
├── requirements.txt             # Python 依赖清单
├── run.py                       # 项目启动入口
├── test_crawler.py              # 爬虫单元测试
├── test_pangu.py                # 模型连接测试
├── test_planner.py              # 规划器测试
└── 架构文档.md                  # 本架构文档
```

-----

## 7\. 系统创新点 (Innovation Highlights)

本系统在传统爬虫的基础上，深度融合了 LLM Agent 与统计学算法，主要创新如下：

1.  **AI 决策委员会 (Multi-Agent Council)**:

      - 摒弃了单一模型的“幻觉”风险，构建了由 **Explorer (激进派)**、**Critic (保守派)** 和 **Moderator (主持人)** 组成的决策架构。
      - 在保证泛化能力（能爬未知网站）的同时，通过多轮辩论获取针对性指导，有效解决了“乱爬”和“漏爬”的问题。

2.  **基于链接聚类的智能分组 (Cluster-based Processing)**:

      - 引入 URL 模式聚类算法，将海量页面压缩为少量的“模板簇”。
      - 这不仅大幅降低了 LLM 上下文窗口的压力，更为后续的**统计学去噪**提供了数据基础。

3.  **跨页面结构评分与统计学去噪 (Cross-page Structural Scoring & Statistical Denoising)**:

      - **去噪 (Denoising)**: 利用“文档频率 (Document Frequency)”算法，物理识别并切除组内页面共有的导航栏、页脚等噪音。无需 Prompt，直接让 AI 看到最纯净的正文。
      - **评分 (Scoring)**: 结合局部结构分（Local Score）与模板稳定性（Template Score），精准识别高价值信息页。
      - **去重 (Deduplication)**: 基于正文内容的指纹哈希，自动跳过 URL 不同但内容重复的页面（如带参链接），节省 Token 成本。

4.  **意图驱动的模板提取 (Intent-Driven Schema Discovery)**:

      - 将用户的自然语言需求（如“提取教授信息”）注入到 Schema 发现阶段。
      - 结合“负面约束” Prompt，强迫 AI 忽略导航菜单，只关注与用户需求强相关的实体，实现了**精准的结构化**，避免了“提取出一堆菜单链接”的尴尬。

5.  **自适应高并发架构 (Adaptive Concurrency & Fallback)**:

      - **密钥轮询**: 支持 API Key Pool，自动轮询多个账号 Key 以突破 Rate Limit。
      - **模型接力**: 实现了模型自动降级策略（Model Fallback）。当主模型（如 Qwen-7B）拥堵报 503 时，毫秒级自动切换至备用模型（如 DeepSeek-V3），确保任务永远不卡死。

-----

## 6\. 更新记录（Update Log）

> 规则：**仅追加，不修改历史记录**；最新记录写在最上方。

### 记录ID: ui-algo-overhaul-001 (Final Polish)

  - 时间: 2025-12-04T16:00:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [D-03, E-01]
  - 主要工作:
      - **算法升级**: 实现了 `_compute_and_remove_noise`，利用统计学原理物理切除网页导航栏，极大提升了 AI 提取的信噪比。
      - **前端重构**: `HomeView.vue` 全面升级，增加了“终端直播”、“结构化数据表格”、“去噪前后对比开关”以及“历史任务记录”功能。
      - **并发优化**: 引入了 `check_keys.py` 体检脚本，并在 `extractor.py` 中实现了基于 Key 数量的自适应高并发 (Concurrency = Keys \* 5) 及模型自动切换逻辑。
  - 结果:
      - 系统成功从北京理工大学网站提取出了干净的教授名单 JSON，验证了“意图驱动 + 统计去噪”路线的有效性。
  - 已更新内容:
      - [x] 5.1 前端任务 D-03 标记完成。
      - [x] 7. 新增系统创新点章节。
  - 建议的下一步:
      - 执行 F-01，将这一整套复杂的环境 Docker 化，方便部署。

### 记录ID: backend-complete-001 (Feature Complete)

  - 时间: 2025-12-04T14:00:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-06, D-02, STRUCTURE-01]
  - 主要工作:
      - **Analyzer 落地**: 实现了 `ResultAnalyzer`。
      - **Schema Discovery 落地**: 实现了基于 URL 聚类的 Schema 自动推导。
      - **Extractor 落地**: 实现了 `StructuredExtractor`，支持并发抽取。


### 记录ID: backend-complete-001 (Feature Complete)

  - 时间: 2025-12-04T14:00:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-06, D-02, STRUCTURE-01]
  - 主要工作:
      - **Analyzer 落地**: 实现了 `ResultAnalyzer`，集成 `structure_score` 逻辑，优先选择高结构分页面进行总结，避免“垃圾进垃圾出”。
      - **Schema Discovery 落地**: 实现了基于 URL 聚类的 Schema 自动推导，盘古模型可自动识别实体类型和字段。
      - **Extractor 落地**: 实现了 `StructuredExtractor`，支持并发抽取，且在 Prompt 层面解决了相对路径还原问题。
      - **Utils 完善**: 实现了基于路径片段的鲁棒性 URL 聚类算法 (`cluster_links`)，为 Planner 和 Discovery 提供分组依据。
  - 结果:
      - 后端所有预想的高级 AI 功能（多模态决策、结构化感知、自动抽取）均已实现代码落地。
  - 已更新内容:
      - [x] 5.1 标记后端任务全清 (All Backend Tasks Checked)
      - [x] 5.3 更新文件树，纳入新模块。
  - 已知问题/限制:
      - `main.py` 中的 `template_scorer` 引用可能多余。
      - 前端目前只能看 Markdown，无法展示 AI 提取出来的精美 JSON 表格。
  - 建议的下一步:
      - 执行 **E-01**：在前端开发“数据透视”功能，将后端提取的结构化数据可视化。

### 记录ID: planning-002 (Sync Tasks & Next Action)

  - 时间: 2025-12-03T22:00:00+08:00
  - 执行主体: human & ai/gpt-5.1-thinking
  - 涉及任务: [5.1 状态同步, 5.2 NEXT\_ACTION 更新]
  - 主要工作:
      - 根据既有更新记录，对齐 5.1 功能模块状态。
      - 在 5.1 中新增 D-02「结构化抽取器实现」任务。
      - 新增当前 NEXT\_ACTION "STRUCTURE-01"。

### 记录ID: extraction-schema-001 (Finalizing Data Flow)

  - 时间: 2025-12-03T21:30:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-06 Analyzer, D-02 Extractor]
  - 主要工作:
      - **效率瓶颈确诊**: 确认 Analyzer 速度瓶颈。
      - **结构化提取设计**: 敲定 Schema Discovery & Local Execution 策略。

### 记录ID: strategy-001 (Major Milestone)

  - 时间: 2025-12-03T20:15:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-02 Planner, B-03 Scheduler]
  - 主要工作:
      - **战略重构**: 确立 **"多智能体决策委员会"** 架构。
      - **角色分工定义**: Explorer (Qwen), Critic (DeepSeek), Moderator (Pangu)。
      - **流程升级**: 确立 "分层爬取 -\> 汇报 -\> AI开会 -\> 决策 -\> 下一层" 模式。

### 记录ID: backend-optimization-002

  - 时间: 2025-12-03T13:10:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-03, B-04]
  - 主要工作:
      - 解决 Windows 异步循环报错。
      - 升级为 BFS 递归爬虫。

### 记录ID: frontend-001

  - 时间: 2025-12-03T11:20:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [D-01]
  - 主要工作:
      - 初始化 Vue 3 + Element Plus 项目。

### 记录ID: fix-db-001

  - 时间: 2025-12-03T10:40:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [INIT-01, B-05]
  - 主要工作:
      - 解决 PostgreSQL 端口冲突 (5432 -\> 5435)。

### 记录ID: setup-006

  - 时间: 2025-12-02T22:30:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-03, C-01]
  - 主要工作:
      - 后端 API 雏形完成，实现 Planner -\> Crawler -\> Storage 全流程。

### 记录ID: setup-005

  - 时间: 2025-12-02T22:00:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-04]
  - 主要工作:
      - 实现 crawler.py，封装 AsyncWebCrawler。

### 记录ID: setup-004

  - 时间: 2025-12-02T21:40:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-01, B-02]
  - 主要工作:
      - 实现 Planner 和 JSON 清洗。

### 记录ID: setup-003

  - 时间: 2025-12-02T21:30:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-01]
  - 主要工作:
      - 封装 Pangu Client。

### 记录ID: init-002

  - 时间: 2025-12-02T21:15:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [INIT-01]
  - 主要工作:
      - 编写 docker-compose.yml 和 models.py。

### 记录ID: setup-001

  - 时间: 2025-12-02T21:05:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [A-02]
  - 主要工作:
      - 技术选型完成。
爬取 https://cs.bit.edu.cn/szdw/jsml/index.htm 里的所有教师信息，包括姓名、职称、邮箱、研究方向
<!-- end list -->

