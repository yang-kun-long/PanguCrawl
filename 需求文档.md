# 深网内容爬虫系统架构（基于 crawl4AI + 盘古大模型）
version: 0.3
status: "BACKEND_COMPLETE"

ai_entry_point:
  read_order:
    - "1. 目标与范围"
    - "2. 整体架构概览"
    - "5. 当前进度 & 下一步计划"
    - "6. 更新记录（最近一条）"
  next_action_procedure:
    - "步骤1：阅读 5.1 的任务列表，确认哪些已完成、哪些未完成。"
    - "步骤2：优先查看 5.2 中的 NEXT_ACTION，如果存在则尝试完成它。"
    - "步骤3：完成任务后，更新 5.1 勾选状态（[ ] -> [x]），并在 6. 更新记录 追加一条记录。"
    - "步骤4：如有新的后续任务，在 5.1 追加新的待办项，并在 5.2 更新新的 NEXT_ACTION。"
  write_back_rules:
    - "仅在完成实质性工作时更新文档，不要为纯思考过程写更新记录。"
    - "更新记录必须包含：时间、执行主体、做了什么、剩余问题/限制。"
    - "保持原有内容不被随意删除，如需废弃某设计，在更新记录中说明原因。"

---

## 1. 目标与范围

### 1.1 核心目标

- G1: 支持按主题进行深网内容爬取（Topic-based crawling）。
- G2: 支持按 URL 进行深网内容爬取（URL-based crawling）。
- G3: 爬取前可通过盘古大模型对话生成/优化爬取计划。
- G4: 爬取后由盘古对结果进行总结、描述、评估和优化建议。
- G5: 支持多模态内容爬取（发现并记录图片/视频等媒体信息）。
- G6: **[NEW]** 支持网页结构化数据自动发现与提取（Schema Discovery & Extraction）。

### 1.2 非目标（当前阶段）

- N1: 暂不实现复杂分布式调度，仅保证单服务 + 简单并发。
- N2: 暂不固定具体数据库，先抽象出需要存储的数据结构。

---

## 2. 整体架构概览

### 2.1 模块划分（Tech Stack Update）

1. **API Gateway / Orchestrator**: Python (FastAPI)
   - 核心后端，负责请求路由、任务调度、状态管理。
2. **LLM Planner (Multi-Agent Council)**: Python Client (调用盘古 API)
   - **Explorer (激进派)**: 负责发现所有潜在链接。
   - **Critic (保守派)**: 负责过滤广告与无关链接。
   - **Moderator (主持人)**: 负责综合意见并下达最终爬取指令。
3. **Crawler Engine**: Python (crawl4AI / Playwright)
   - 纯异步爬虫核心，负责执行具体页面的渲染和抓取。
4. **Structure & Schema Engine**: Python
   - **Structure Scorer**: 对网页结构化程度打分。
   - **Schema Discovery**: 自动聚类 URL 并推导数据 Schema。
   - **Extractor**: 基于 Schema 进行本地并发数据抽取。
5. **Storage Layer**: PostgreSQL
   - 使用 `JSONB` 字段存储 CrawlPlan、PageCrawlResult 和 ExtractedData。
6. **Result Analyzer**: Python (后台异步任务)
   - 负责对爬取回来的原始数据进行清洗和 LLM 总结。
7. **Frontend**: Vue.js 3
   - 用户界面，用于输入任务、查看进度（含辩论直播）、结果和提取的数据表格。

### 2.2 逻辑数据流

```text
[User / Vue Client]
        | (HTTP/REST)
        v
[FastAPI Gateway]
        |
        +--> [LLM Planner - Multi-Agent Debate] --> CrawlPlan (JSON + Debate Logs)
        |
        v
[Crawl Scheduler (Recursive Loop)] --> URL 队列 --> [Crawler Engine]
        |
        v
[Raw Page Results] --> [Structure Scorer] --> [PostgreSQL (JSONB)]
        |
        +--> [Schema Discovery] --> Schema Definitions
        |
        +--> [Structured Extractor] --> Extracted Data (JSON)
        |
        v
[Result Analyzer] --> Final Summary
        |
        v
[User / Vue Client]
````

-----

## 3\. 数据与协议（JSON 结构）

### 3.1 CrawlPlan (扩充)

```jsonc
{
  "plan_id": "string",
  "topic": "string",
  "seed_urls": ["string"],
  "max_depth": 2,
  "max_pages": 100,
  "debate_logs": "string (Markdown log of AI council)",
  "schemas": [
    {
       "template_pattern": "/news/{id}",
       "entity_type": "news_article",
       "fields": [...]
    }
  ],
  "structured_extract": {
     "task_id": "...",
     "templates": [
        {
           "template_pattern": "...",
           "records": [{"field1": "val1"}, ...]
        }
     ]
  }
}
```

### 3.2 CrawlTask

```jsonc
{
  "task_id": "string",
  "plan_id": "string",
  "status": "PENDING",
  "mode": "topic",
  "request_type": "CREATE_PLAN_AND_RUN",
  "user_request": "string",
  "created_at": "ISO8601",
  "updated_at": "ISO8601"
}
```

### 3.3 PageCrawlResult

```jsonc
{
  "task_id": "string",
  "plan_id": "string",
  "url": "string",
  "status": "SUCCESS",
  "http_status": 200,
  "markdown": "string",
  "html": "string",
  "links": {
    "internal": ["string"],
    "external": ["string"]
  },
  "media": {
    "images": ["string"],
    "videos": ["string"],
    "audios": ["string"],
    "structure": {
       "local_structure_score": 0.8,
       "template_score": 0.9,
       "structure_score": 0.85
    }
  },
  "meta": {
    "title": "string",
    "language": "string",
    "detected_topic": "string"
  },
  "error_message": "string | null",
  "fetched_at": "ISO8601"
}
```

### 3.4 TaskAnalysisResult

```jsonc
{
  "task_id": "string",
  "plan_id": "string",
  "summary": "string",
  "key_points": ["string"],
  "source_types": ["string"],
  "quality_score": 0,
  "quality_comment": "string",
  "next_crawl_suggestions": ["string"],
  "created_at": "ISO8601"
}
```

-----

## 4\. 服务接口（逻辑）

### 4.1 创建并启动爬取任务

```http
POST /tasks
```

```jsonc
Request {
  "mode": "topic" | "url",
  "user_request": "string",
  "seed_urls": ["string"],
  "options": {
    "max_depth": 2,
    "max_pages": 50
  }
}
```

```jsonc
Response {
  "task_id": "string",
  "plan_id": "string",
  "status": "PENDING"
}
```

### 4.2 查询任务状态

```http
GET /tasks/{task_id}
```

*Response 包含 `debate_logs`, `results`, `summary`, `crawled_count` 等字段。*

### 4.3 获取任务分析结果

```http
GET /tasks/{task_id}/analysis
```

-----

## 5\. 当前进度 & 下一步计划

### 5.1 功能模块状态

#### 后端核心 (Backend)

  - [x] A-01 架构设计 & A-02 技术选型 & A-03 数据库 Schema
  - [x] B-01 盘古 Client 封装
  - [x] **B-02 LLM Planner** (多智能体决策委员会: Explorer/Critic/Moderator)
  - [x] **B-03 Crawl Scheduler** (分层递归 + AI 辩论调度)
  - [x] **B-04 crawl4AI 接入与封装**
  - [x] **B-05 数据存储** (PostgreSQL + JSONB)
  - [x] **B-06 Result Analyzer** (基于结构化评分的加权总结)
  - [x] **D-02 结构化抽取器** (Schema 自动发现 + 本地并发抽取)
  - [x] **STRUCTURE-01 结构化探测** (Local + Template 评分系统)

#### 服务与监控 (Service)

  - [x] C-01 API Gateway 基础工程
  - [ ] C-02 基础监控与日志 (目前仅有控制台日志)

#### 前端 (Frontend)

  - [x] D-01 前端项目初始化
  - [ ] **D-03 深度数据展示** (需对接：辩论直播、结构化数据表格、Schema展示)

### 5.2 下一步推荐行动（NEXT\_ACTION）

NEXT\_ACTION:
id: "E-01"
title: "端到端联调与可视化对接"
description: |

1.  **后端验证**: 挑选一个真实目标（如学校新闻网），运行一次完整任务，观察控制台日志：
      - 确认 `debate_stream` 是否正常产出激进派/保守派的对话。
      - 确认 `schema_discovery` 是否生成了合理的字段定义。
      - 确认 `extractor` 是否提取到了 JSON 数据。
2.  **前端升级 (`HomeView.vue`)**:
      - **辩论直播**: 解析 `task.debate_logs`，用类似聊天窗口的形式展示 AI 会议过程。
      - **结构化数据展示**: 在结果页增加一个 "Extracted Data" Tab，渲染 `task.plan.structured_extract` 中的表格数据。
      - **下载功能**: 允许用户将提取的 JSON/CSV 下载到本地。
        depends\_on: ["B-06", "D-02"]
        expected\_output: "用户可以在网页上看到 AI 们在'开会'，并且任务结束后能直接看到提取好的 Excel/JSON 表格，而不仅仅是 Markdown 文本。"

### 5.3 现有项目架构

```text
📂 项目架构: 盘古爬虫
├── app
│   ├── services
│   │   ├── analyzer.py          # [DONE] 最终结果分析与总结
│   │   ├── crawler.py           # [DONE] 网页抓取核心
│   │   ├── extractor.py         # [DONE] 结构化数据抽取器
│   │   ├── llm.py               # [DONE] 盘古大模型客户端
│   │   ├── planner.py           # [DONE] 多智能体决策委员会
│   │   ├── schema_discovery.py  # [DONE] 结构模式自动发现
│   │   ├── structure_scorer.py  # [DONE] 页面结构评分系统
│   │   └── template_scorer.py   # (已废弃，功能合并至 structure_scorer)
│   ├── __init__.py
│   ├── config.py
│   ├── database.py
│   ├── main.py                  # [DONE] API 入口与业务编排
│   ├── models.py                # [DONE] 数据库模型
│   └── utils.py                 # [DONE] URL 聚类与清洗工具
├── web-ui
│   ├── src
│   │   ├── views
│   │   │   └── HomeView.vue     # [待升级] 前端主视图
│   │   └── ...
├── .env
├── docker-compose.yml
└── run.py
```

-----

## 6\. 更新记录（Update Log）

> 规则：**仅追加，不修改历史记录**；最新记录写在最上方，方便 AI 快速获取最近状态。

### 记录ID: backend-complete-001 (Feature Complete)

  - 时间: 2025-12-04T14:00:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-06, D-02, STRUCTURE-01]
  - 主要工作:
      - **Analyzer 落地**: 实现了 `ResultAnalyzer`，集成 `structure_score` 逻辑，优先选择高结构分页面进行总结，避免“垃圾进垃圾出”。
      - **Schema Discovery 落地**: 实现了基于 URL 聚类的 Schema 自动推导，盘古模型可自动识别实体类型和字段。
      - **Extractor 落地**: 实现了 `StructuredExtractor`，支持并发抽取，且在 Prompt 层面解决了相对路径还原问题。
      - **Utils 完善**: 实现了基于路径片段的鲁棒性 URL 聚类算法 (`cluster_links`)，为 Planner 和 Discovery 提供分组依据。
  - 结果:
      - 后端所有预想的高级 AI 功能（多模态决策、结构化感知、自动抽取）均已实现代码落地。
  - 已更新内容:
      - [x] 5.1 标记后端任务全清 (All Backend Tasks Checked)
      - [x] 5.3 更新文件树，纳入新模块。
  - 已知问题/限制:
      - `main.py` 中的 `template_scorer` 引用可能多余。
      - 前端目前只能看 Markdown，无法展示 AI 提取出来的精美 JSON 表格。
  - 建议的下一步:
      - 执行 **E-01**：在前端开发“数据透视”功能，将后端提取的结构化数据可视化。

### 记录ID: planning-002 (Sync Tasks & Next Action)

  - 时间: 2025-12-03T22:00:00+08:00
  - 执行主体: human & ai/gpt-5.1-thinking
  - 涉及任务: [5.1 状态同步, 5.2 NEXT\_ACTION 更新]
  - 主要工作:
      - 根据既有更新记录，对齐 5.1 功能模块状态。
      - 在 5.1 中新增 D-02「结构化抽取器实现」任务。
      - 新增当前 NEXT\_ACTION "STRUCTURE-01"。

### 记录ID: extraction-schema-001 (Finalizing Data Flow)

  - 时间: 2025-12-03T21:30:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-06 Analyzer, D-02 Extractor]
  - 主要工作:
      - **效率瓶颈确诊**: 确认 Analyzer 速度瓶颈。
      - **结构化提取设计**: 敲定 Schema Discovery & Local Execution 策略。

### 记录ID: strategy-001 (Major Milestone)

  - 时间: 2025-12-03T20:15:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-02 Planner, B-03 Scheduler]
  - 主要工作:
      - **战略重构**: 确立 **"多智能体决策委员会"** 架构。
      - **角色分工定义**: Explorer (Qwen), Critic (DeepSeek), Moderator (Pangu)。
      - **流程升级**: 确立 "分层爬取 -\> 汇报 -\> AI开会 -\> 决策 -\> 下一层" 模式。

### 记录ID: backend-optimization-002

  - 时间: 2025-12-03T13:10:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [B-03, B-04]
  - 主要工作:
      - 解决 Windows 异步循环报错。
      - 升级为 BFS 递归爬虫。

### 记录ID: frontend-001

  - 时间: 2025-12-03T11:20:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [D-01]
  - 主要工作:
      - 初始化 Vue 3 + Element Plus 项目。

### 记录ID: fix-db-001

  - 时间: 2025-12-03T10:40:00+08:00
  - 执行主体: human & ai
  - 涉及任务: [INIT-01, B-05]
  - 主要工作:
      - 解决 PostgreSQL 端口冲突 (5432 -\> 5435)。

### 记录ID: setup-006

  - 时间: 2025-12-02T22:30:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-03, C-01]
  - 主要工作:
      - 后端 API 雏形完成，实现 Planner -\> Crawler -\> Storage 全流程。

### 记录ID: setup-005

  - 时间: 2025-12-02T22:00:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-04]
  - 主要工作:
      - 实现 crawler.py，封装 AsyncWebCrawler。

### 记录ID: setup-004

  - 时间: 2025-12-02T21:40:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-01, B-02]
  - 主要工作:
      - 实现 Planner 和 JSON 清洗。

### 记录ID: setup-003

  - 时间: 2025-12-02T21:30:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [B-01]
  - 主要工作:
      - 封装 Pangu Client。

### 记录ID: init-002

  - 时间: 2025-12-02T21:15:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [INIT-01]
  - 主要工作:
      - 编写 docker-compose.yml 和 models.py。

### 记录ID: setup-001

  - 时间: 2025-12-02T21:05:00+09:00
  - 执行主体: ai/gemini-1.5-pro
  - 涉及任务: [A-02]
  - 主要工作:
      - 技术选型完成。
爬取 https://cs.bit.edu.cn/szdw/jsml/index.htm 里的所有教师信息，包括姓名、职称、邮箱、研究方向
<!-- end list -->

